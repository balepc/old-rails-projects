<% content_for :sub_menu do %>
  <%= link_to image_tag('admin/page_text.gif')+'Назад к списку', admin_document_types_path %>
<% end %>

<% form_for @document_type, :url=>admin_document_type_path(@document_type), :html=>{:multipart=>true} do |form| %>
  <table class="full-form">
    <colgroup width="100" />

    <%= render :partial => 'form', :locals=>{:form=>form}  %>

    <tr>
      <td colspan="2" class="form-controls">
        <hr />
        <%= form.submit 'Обновить' %> <%= form.submit 'Обновить и вернуться к списку' %>
      </td>
    </tr>
  </table>
<% end %>